import { classSession } from '../classes/session/session.ts';

/* The line `const session = new classSession();` is creating a new instance of the `classSession`
class and assigning it to the constant variable `session`. This allows you to use the `session`
object to access the properties and methods defined in the `classSession` class. */
export const session = new classSession();
