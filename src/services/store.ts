import { classStore } from '../classes/store/store.ts';

/* The line `const store = new classStore();` is creating a new instance of the `classStore` class and
assigning it to the `store` constant. This allows you to access and use the methods and properties
of the `classStore` class through the `store` object. */
export const store = new classStore();
